<%= form_for(@atc) do |f| %>
  <% if @atc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@atc.errors.count, "error") %> prohibited this atc from being saved:</h2>

      <ul>
      <% @atc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :nimetus_est %><br />
    <%= f.text_field :nimetus_est %>
  </div>
  <div class="field">
    <%= f.label :nimetus_eng %><br />
    <%= f.text_field :nimetus_eng %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
